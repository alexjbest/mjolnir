<?xml version="1.0" encoding="UTF-8" ?>

<!--********************************************************************

Copyright 2014 Alex J. Best

*********************************************************************-->

<mathbook>

  <docinfo>
    <brandlogo url="http://alexjbest.github.io/mjolnir/" source="../../images/mjolnir.png" />
    <author>
      <personname>Dr. Jacob Rasmussen</personname>
      <department>DPMMS</department>
      <institution>University of Cambridge</institution>
    </author>
    <author>
      <personname>Alex J. Best</personname>
      <department>DPMMS</department>
      <institution>University of Cambridge</institution>
      <email>alex.j.best@gmail.com</email>
    </author>

    <macros>
    \DeclareMathOperator{\Map}{Map}
    \DeclareMathOperator{\Hom}{Hom}
    \DeclareMathOperator{\im}{im}
    \DeclareMathOperator{\id}{id}
    \DeclareMathOperator{\coker}{coker}
    \DeclareMathOperator{\CC}{\mathbf{C}}
    \DeclareMathOperator{\QQ}{\mathbf{Q}}
    \DeclareMathOperator{\RR}{\mathbf{R}}
    \DeclareMathOperator{\ZZ}{\mathbf{Z}}
    \DeclareMathOperator{\RP}{\mathbf{RP}}
    \DeclareMathOperator{\C}{C_*}
    \DeclareMathOperator{\U}{\mathcal{U}}
    </macros>

    <initialism>AT</initialism>

    <feedback>
      <url>https://github.com/alexjbest/mjolnir/issues</url>
    </feedback>

    <date><today /></date>
  </docinfo>

  <book xml:id="algtop">
    <title>Part III Algebraic Topology 2014</title>

    <chapter>
      <title>Homology</title>

      <section>
        <title>Introduction</title>

        <p>
          These are lecture notes for the 2014 Part III Algebraic Topology course taught by Dr. Jacob Rasmussen.
        </p>
        <p>
          The recommended books are:
          <ul>
            <li><url href="http://www.math.cornell.edu/~hatcher/AT/ATpage.html">Algebraic Topology</url> - Allen Hatcher,</li>
            <li>Homology Theory - James W. Vick,</li>
            <li>Differential Forms in Algebraic Topology - Raoul Bott and Loring W. Tu.</li>
          </ul>
        </p>

        <p>
          Generated: <today />, <timeofday />
        </p>
      </section>

      <section>
        <title>Homotopy</title>

        <subsection>
          <title>Homotopies</title>

          <definition>
            <title>Homotopic maps</title>
            <statement>
              Maps <m>f_0,f_1\colon X \to Y</m> are said to be <term>homotopic</term> if there is a
              continuous map <m>F\colon X\times I \to Y</m> such that
              <me>
                F(x,0) = f_0(x)\text{ and }F(x,1) = f_1(x)\ \forall x\in X.
              </me>
            </statement>
          </definition>

          <p>
            We let <m>\Map(X,Y) = \{f\colon X \to Y \text{ continuous}\}</m>.
            Then letting <m>f_t(x) = F(x,t)</m> in the above definition we see that <m>f_t</m> is a
            path from <m>f_0</m> to <m>f_1</m> in <m>\Map(X,Y)</m>.
          </p>

          <example>
            <ol>
              <li><m>X = Y = \RR^n</m>, <m>f_0(\overline{x}) = \overline{0}</m> and <m>f_1(\overline{x}) = \overline{x}</m> are homotopic via <m>f_t(\overline{x}) = t\overline{x}</m>.</li>
              <li><m>S^1 = \{z\in \CC : |z| = 1\}</m> then <todo></todo></li>
              <li><m>S^n = \{ \overline{x} \in \RR^n : |\overline{x}| = 1\}</m> <todo></todo></li>
            </ol>
          </example>

          <lemma>
            <statement>
              Homotopy is an equivalence relation on <m>\Map(X,Y)</m>.
            </statement>
          </lemma>

          <notation>
            <me>
              [X,Y] = \Map(X,Y)/\sim = \text{set of homotopy classes of maps } X \to Y.
            </me>
          </notation>

          <lemma>
            <statement>
              If <m>f_0 \sim f_1\colon X \to Y</m> and <m>g_0 \sim g_1\colon Y \to Z</m> then
              <m>g_0\circ f_0 \sim g_1\circ f_1</m>.
            </statement>
          </lemma>

          <corollary>
            <statement>
              For any space <m>X</m> the set <m>[X,\RR^n]</m> has one element.
            </statement>
            <proof>
              Define <m>0_X\colon X \to \RR^n</m> by <m>0_X(x) = 0 \in \RR^n</m> for any <m>x\in X</m>.
            </proof>
          </corollary>

          <definition>
            <title>Contractible space</title>
            <statement>
              <m>X</m> is <term>contractible</term> if <m>1_X</m> is homotopic to a constant map.
            </statement>
          </definition>

          <proposition>
            <statement>
              <m>Y</m> is contractible <m>\iff</m> <m>[X,Y]</m> has one element for any space <m>X</m>.
            </statement>

            <proof>
              (<m>\Rightarrow</m>) as in corollary.  (<m>\Leftarrow</m>) <m>[X,Y]</m> has one element so
              <m>1_Y \sim </m> a constant map.
            </proof>
          </proposition>

          <p>
            Given a space <m>X</m> how can we tell if <m>X</m> is contractible? If <m>X</m> is
            contractible then it must be path connected for one.

            <proof>
              Contractible implies that <m>[S^0, X]</m> has one element and so <m>f \colon
              S^0 \to X</m> extends to <m>D^1</m>, and therefore <m>X</m> is path connected. <todo></todo>
            </proof>

            Similarly if <m>[S^1, X]</m> has more than one element then <m>X</m> is not
            contractible.
          </p>

          <definition>
            <title>Simply connected</title>
            <statement>
              We say <m>X</m> is <term>simply connected</term> if <m>[S^1, X]</m> has only one
              element.

              We say two space <m>X</m> and <m>Y</m> are <em>homotopy equivalent</em> if there
              exists <m>f\colon X \to Y</m> and <m>g\colon Y \to X</m> such that <m>g\circ f
              \sim 1_X</m> and <m>f\circ g \sim 1_Y</m>.
            </statement>
          </definition>

          <example>
            <p>
              <m>X</m> is contractible if and only if <m>X \sim \{p\}</m>.
            </p>
            <proof>
              <m>X</m> contractible <m>\implies 1_X \sim c</m>, a constant map.
              Choose <m>f\colon X \to \{p\}</m>, <m>f(x) = p</m> and <m>g\colon\{p\} \to X</m>,
              <m>g(p) = c</m>. Then <m>g\circ f = c \sim 1</m> and <m>f\circ g = 1_{\{p\}}</m>.
              Converse: exercise.
            </proof>
          </example>

          <exercise>
            If <m>X_0 \sim X_1</m> and <m>Y_0 \sim Y_1</m> then <m>[X_0, Y_0]</m> and <m>[X_1,
            Y_1]</m> are in bijection.
          </exercise>

          <p>
            Given <m>X</m> and <m>Y</m> how can we determine if <m>X\sim Y</m>?
            How do we determine <m>[X,Y]</m>?
            For example is <m>S^n \sim S^m</m>.
          </p>

        </subsection>

        <subsection>
          <title>Homotopy groups</title>

          <definition>
            <title>Map of pairs</title>
            <statement>
              A <term>map of pairs</term> <m>f\colon (X, A) \to (Y, B)</m> is a map <m>f\colon X
              \to Y</m> with sets <m>A\subset X</m> and <m>B\subset Y</m> such that <m>f(A)\subset
              B</m>.

              If we have maps of pairs <m>f_0, f_1\colon (X,A) \to (Y,B)</m> then we
              write <m>f_0\sim f_1</m> if there exists <m>F\colon(X\times I, A\times I) \to
              (Y,B)</m> such that <m>F(x,0) = f_0(x)</m> and <m>F(x,1) = f_1(x)</m>.
            </statement>
          </definition>

          <definition>
            <title>Homotopy groups</title>
            <statement>
              If <m>*\in X</m> then the <term><m>n</m>th homotopy group</term> is
              <me>\pi_n(X, *) = [(D^n, S^{n-1}) \to (X, \{*\})].</me>
            </statement>
          </definition>

          <p>
            We now note several properties of this definition:
            <ol>
              <li><m>\pi_0(X, *) =</m> set of path components of <m>X</m>.</li>

              <li><m>\pi_1(X, *)</m> is a group.</li>

              <m>\pi_n(X, *)</m> is an abelian group. <todo></todo>

              <li><m>\pi_n</m> is a functor
                <me>
                  \left\{ \begin{subarray}{l}\text{pointed spaces} \\ \text{pointed
                  maps} \end{subarray}\right\} \to \left\{\begin{subarray}{l}
                  \text{groups}\\ \text{group homomorphisms}\end{subarray}\right\}.
                </me>
                So given
                <me>f\colon(X, p)\to(Y,q)</me>
                we get
                <me>f_*\colon \pi_n(X,p)\to\pi_n(y,q)</me>
                defined by
                <me>f_*(\gamma) = f\circ \gamma.</me>
                <todo>maybe id</todo>
              </li>
            </ol>
          </p>

          <example>
            <title>Homotopy groups of <m>S^2</m></title>
            <table xml:id="table-euler1">
              <tgroup cols="8" align="center">
              <tbody>
              <row><entry><m>n</m></entry><entry>1</entry><entry>2</entry><entry>3</entry><entry>4</entry><entry>5</entry><entry>6</entry><entry>7</entry></row>
              <row><entry><m>\pi_n(S^2)</m></entry><entry>0</entry><entry><m>\ZZ</m></entry><entry><m>\ZZ</m></entry><entry><m>\ZZ/2</m></entry><entry><m>\ZZ/2</m></entry><entry><m>\ZZ/12</m></entry><entry><m>\ZZ/15</m></entry></row>
              </tbody>
              </tgroup>
            </table>
          </example>
        </subsection>
      </section>

      <section>
        <title>Homology</title>
        <p>
          Our goal is to construct a functor <m>H_*</m> from the category of topological spaces and continuous maps to the category of <m>\ZZ</m>-modules and <m>\ZZ</m>-linear maps.
          This means to each space <m>X</m> we associate an abelian group <m>H_*(X) = \bigoplus_{n\ge 0}H_n(X)</m>, and to each map <m>f\colon X \to Y</m> a function <m>f_*\colon H_n(X) \to H_n(Y)</m> satisfying <m>(1_X)_* = 1_{H_n(X)}</m> and <m>(f\circ g)_* = f_* \circ g_*</m>.
        </p>
        <p>
          Some properties we would like to have for our construction are:
          <ol>
            <li>Homotopy invariance, if <m>f \sim g \colon X \to Y</m> then <m>f_* = g_*</m>.</li>
            <li>The dimension axiom, <m>H_n(X) = 0</m> for any <m>n > \dim X</m>.</li>
          </ol>
        </p>

        <subsection>
          <title>Chain complexes</title>

          <definition>
            <title>Chain complex</title>
            <statement>
              If <m>R</m> is a commutative ring then a <term>chain complex</term> over <m>R</m> is a pair <m>(C,d)</m> satisfying:
              <ol>
                <li><m>C = \bigoplus_{n\in\ZZ} C_n</m> for <m>R</m>-modules <m>C_n</m>.</li>
                <li><m>d\colon C \to C</m> where <m>d = \bigoplus d_n</m> for <m>R</m>-linear maps <m>d_n</m>.</li>
                <li><m>d\circ d = 0</m>.</li>
              </ol>
              The indexing by <m>n</m> is called a <term>grading</term>.
              Usually we take <m>C_n = 0</m> for <m>n \lt 0</m>.
              An element of <m>\ker d_n</m> is called <term>closed</term> or a <term>cycle</term>.
              An element of <m>\im d_n</m> is called a <term>boundary</term>.
              <m>d</m> is the <term>boundary map</term> or <term>differential</term>.
            </statement>
          </definition>

          <definition>
            <title>Homology groups</title>
            <statement>
              If <m>(C,d)</m> is a chain complex, its <term><m>n</m>th homology group</term> is
              <me>H_n(C,d) = \ker d_n/\im d_{n+1}.</me>
              If <m>x \in \ker d_n</m> we write <m>[x]</m> for its image in <m>H_n(C)</m>.
            </statement>
          </definition>

          <example>
            <ol>
              <li>
                <m>C_0 = C_1 = \ZZ</m>, <m>C_i = 0</m> otherwise,
                <me>
                  0\to \ZZ \xrightarrow{\cdot 3} \ZZ \to 0.
                </me>
                Then <m>H_1 = 0</m>, <m>H_0 = \ZZ/3</m>.
              </li>
              <li>
                <me>
                  \ZZ=\langle e\rangle\to\ZZ^2 = \langle f_1,f_2\rangle\to\ZZ=\langle g\rangle \to 0
                </me>
                with <m>d(e) = f_1-f_2</m>, <m>d(f_1) = d(f_2) = g</m>, then <m>H_*(C) = 0</m>(exercise).
              </li>
            </ol>
          </example>
        </subsection>

        <subsection>
          <title>The chain complex of a simplex</title>

          <definition>
            <title><m>n</m>-simplex</title>
            <statement>
              The <term><m>n</m>-dimensional simplex</term> <m>\Delta^n</m> is <me>\Delta^n = \left\{ (x_0,\ldots,x_n)\in\RR^n : \sum_{i} x_i = 1,\, x_i \ge 0 \forall i\right\}.</me>
              <m>\Delta^n</m> has <term>vertices</term> <m>v_0,\ldots,v_n</m> which are the intersections with the coordinate axes. The <m>k</m>-dimensional <term>faces</term> are in bijection with the <m>k+1</m>element subsets of <m>\{0,\ldots,n\}</m>.
            </statement>
          </definition>

          <todo>examples with pics</todo>

          <definition>
            <title>Simplicial chain complex</title>
            <statement>
              <m>S_*(\Delta^n)</m> is the chain complex with <m>S_k(\Delta^n)</m> the free <m>\ZZ</m>-module generated by the <m>k</m>-dimensional faces of <m>\Delta^n</m>.
              So
              <me>
                S_k(\Delta^n) = \langle e_I : I = \{i_0,\ldots,i_k : 0 \le i_0 \le \cdots \le i_k \le n \}\rangle.
              </me>
              To define <m>d</m> it suffices to define <m>d(e_I)</m>, we let <me>d(e_I) = \sum_{j = 0}^{k}(-1)^j e_{i_0,\ldots,i_{j-1}, i_{j+1},\ldots,i_k}\in S_{k-1}(\Delta^n).</me>
            </statement>
          </definition>

          <example>
            <ol>
            <li>For <m>\Delta^1</m> we have <m>d(e_{0,1}) = e_1 - e_0</m>.</li>
            <li>For <m>\Delta^2</m> we have <m>d(e_{0,1,2}) = e_{12} - e_{02} + e_{01}</m> and so <m>d^2(e_{I} = (e_2 - e_1) - (e_2 - e_0) + (e_1 - e_0) = 0</m>.</li>
            </ol>
          </example>

          <lemma>
            <statement>
              <m>d^2 = 0</m>
            </statement>
            <proof>
              It suffices to show <m>d^2(e_I) = 0</m> for all <m>I</m>.
              <md>
                <mrow>d^2(e_I) &amp;= d\left(\sum_{j = 0}^{k} (-1)^j e_{i_0,\ldots,\hat{i}_j,\ldots,i_k}\right)</mrow>
                <mrow>&amp;= \sum_{j = 0}^{k} (-1)^j d\left(e_{i_0,\ldots,\hat{i}_j,\ldots,i_k} \right)</mrow>
                <mrow>&amp;= \sum_{j = 0}^{k} (-1)^j\left( \sum_{l \lt j} (-1)^l e_{i_0,\ldots,\hat{i}_l,\ldots,\hat{i}_j,\ldots,i_k} + \sum_{l \gt j} (-1)^{l-1} e_{i_0,\ldots,\hat{i}_j,\ldots,\hat{i}_l,\ldots,i_k}\right)</mrow>
                <mrow>&amp;= \sum_{j = 0}^{k} \left( \sum_{l \lt j} (-1)^{l+j} e_{I - i_l - i_j} - \sum_{l \gt j} (-1)^{l+j} e_{I - i_j - i_l}\right) = 0.</mrow>
                <todo></todo>
              </md>
            </proof>
          </lemma>

          <example>
            <title>Computing <m>H_*(S_*(\Delta^2))</m></title>
            <p>
              <me>
                0\to \ZZ = \langle e_{012}\rangle \to \ZZ^3 = \langle e_{01}, e_{02},e_{12}\rangle \to \ZZ^3 = \langle e_0,e_1,e_2 \rangle \to 0
              </me>
              with <m>d(e_{012}) = e_{12} - e_{02} + e_{01}</m>.
              So <m>\ker d_2 = 0 \implies H_2 = 0</m>.
              <me>
                d(ae_{01} + be_{02} + ce_{12}) = a(e_1 - e_0) + b (e_2 - e_1)  + c(e_2 - e_0)
              </me>
              so <m>ae_{01} + be_{02} + ce_{12}\in \ker d_1 \iff -a-b=0,\,a-c = 0,\, c+b = 0 \iff a = -b = c</m> hence <m>\ker d_1 = \langle e_{01} - e_{02} + e_{12} = \im d_2</m> and so <m>H_1 = 0</m>.
              <m>\ker(d_0) = 0</m>, <m>\im d_1 = \langle e_1 - e_0, e_2 - e_1, e_2 - e_0\rangle</m> so <m>H_0 = \ZZ = \langle [e_0]\rangle</m>.
            </p>
          </example>

          <exercise>
            <statement>
              Show that <m>H_*(S_*(\Delta^n)) = 0</m> if <m>k \ne 0</m> and <m>\ZZ</m> if <m>k = 0</m>.
            </statement>
          </exercise>

        </subsection>

        <subsection>
          <title>The singular chain complex</title>
          <definition>
            <title>Singular chain complex</title>
            <statement>
              If <m>X</m> is a space, the <term>singular chain complex</term> of <m>X</m>, <m>C_*(X)</m> is defined by
              <me>C_n(x) = \langle e_\sigma : \sigma \colon \Delta^n \to X \text{ any continuous map}\rangle.</me>
              Where 
              <me>d(e_\sigma) = \sum_{j=0}^{n} (-1)^j e_{\sigma\circ F_j} \in C_{n-1}(X)</me>
              where <m>F_j\colon \Delta^{n-1} \to \Delta^n</m> is given by <m>F_j(x_0,\ldots,x_{n_1} = (x_0,\ldots,0,\ldots,x_{n-1})</m> with the <m>0</m> in the <m>j</m>th place.
            </statement>
          </definition>

          <todo>missed section</todo>

          <subsubsection>
            <title>Homotopy invariance</title>

            <definition>
              <title>Chain homotopic maps</title>
              <statement>
              Suppose <m>\phi,\psi\colon C_* \to C_*'</m> are chain maps, we say that <m>\phi</m> are <term>chain homtopic</term> if there exists an <m>R</m>-linear map <m>h\colon C_* \to C_{*+1}'</m> such that <m>d'\circ h + h \circ d' = \phi - \psi</m>.
              We denote this relation by <m>\phi \sim \psi</m>.
              </statement>
            </definition>

            <lemma>
              <statement>
                If <m>\phi \sim \psi</m> then <m>\phi_* = \psi_*</m>.
              </statement>
              <proof>
                <md>
                  <mrow>\phi_*([x]) - \psi_*([x]) &amp;= [\phi(x) - \psi(x)]</mrow>
                  <mrow>&amp;= [d'hx + hdx] = [d'hx] = 0 \in H_*(C').</mrow>
                </md>
              </proof>
            </lemma>

            <theorem>
              <statement>
                Suppose <m>f\sim g\colon X\to Y</m> via <m>H</m> then <m>f_\# \sim g_\# \implies f_* = g_*</m>.
              </statement>
              <proof>
                <todo></todo>
              </proof>
            </theorem>

            <corollary>
              <statement>
                If <m>X\sim Y</m> then <m>H_*(X) \cong H_*(Y)</m>.
              </statement>
            </corollary>

            <corollary>
              <statement>
                If <m>X</m> is contractible then <m>H_*(X) \cong H_*(\{p\}) \cong \ZZ</m> if <m>* = 0</m>, <m>0</m> otherwise.
              </statement>
            </corollary>
          </subsubsection>
        </subsection>
      </section>

      <section>
        <title>Homology of a pair</title>

        <subsection>
          <title>Exact sequences</title>

          <definition>
            <title>Exact sequence</title>
            <statement>
              A sequence
              <me>
                \cdots \to A_{n+1} \to A_n \to A_{n-1} \to \cdots
              </me>
              of <m>R</m>-modules and <m>R</m>-linear maps is <term>exact at <m>A_n</m></term> if <m>\ker f_n = \im f_{n+1}</m>.
              <br />
              A sequence is <term>exact</term> if it is exact at all <m>A_n</m>, then <m>(A_*,f)</m> is known as a <term>acyclic</term> chain complex (the homology is zero).
            </statement>
          </definition>

          <example>
            <ol>
              <li>
                <m>0 \to A \xrightarrow{f} B</m> is exact if and only if <m>f</m> is surjective.
              </li>
              <li>
                <m>B \xrightarrow{g} C \to 0</m> is exact if and only if <m>g</m> is injective.
              </li>
              <li>
                <m>0 \to A \xrightarrow{f} A' \to 0</m> is exact if and only if <m>f</m> is an isomorphism.
              </li>
              <li>
                <m>0 \to A \to B \to C \to 0</m> is exact if and only if <m>A\subset B</m> and <m>C\cong B/A</m>.
              </li>
            </ol>
          </example>

          <definition>
            <title>Short exact sequence</title>
            <statement>
              A sequence 
              <me>
                0 \to A_* \xrightarrow i B_* \xrightarrow{\pi} C_* \to 0
              </me>
              is a <term>short exact sequence</term> of chain complexes if
              <ol>
                <li>
                  <m>A_*,B_*,C_*</m> are chain complexes.
                </li>
                <li>
                  <m>i, \pi</m> are chain maps.
                </li>
                <li>
                  <me>
                    0 \to A_n \xrightarrow i B_n \xrightarrow{\pi} C_n \to 0
                  </me>
                  is exact for all <m>n</m>.
                </li>
              </ol>
            </statement>
          </definition>

          <lemma>
            <title>Snake lemma</title>
            <statement>
              If
              <me>
                0 \to A_* \xrightarrow i B_* \xrightarrow{\pi} C_* \to 0
              </me>
              is a short exact sequence of chain complexes then there is an associated <term>long exact sequence</term> of homology groups
              <figure>
                <me>
                  \xymatrix{ \cdots\ar[r] &amp; H_n(A) \ar[r]&amp;
                  H_n(B) \ar[r] &amp; H_n(C)\ar `r[d] `[l]
                  `[llld]_{\partial_n} `[dll] [dll]\\
                  &amp; H_{n-1}(A) \ar[r] &amp; H_{n-1}(B)
                  \ar[r] &amp; H_{n-1}(C)\ar[r] &amp; {\dots} }
                </me>
                <todo>fix tikz/use xyjax</todo>
              </figure>
              The map <m>\partial</m> is called the <term>boundary map</term> in the exact sequence.
            </statement>
            <proof>
              <todo>diagram here</todo>
              We first define <m>\partial</m>, given <m>[c] \in H_n(C)</m> pick <m>b\in B_n</m> such that <m>\pi(b) = c</m>.
              Now <m>\pi db = d\pi b = dc = 0</m> and so <m>db = ia</m> for some <m>a \in A_{n-1}</m>.
              We have that <m>ida = dia = ddb = 0</m> and so <m>da = 0</m> and we can define <m>\partial [c] = [a] \in H_{n-1} (A)</m>. <br />
              We must check that the definition of <m>\partial</m> does not depend on the choice of <m>b</m> or <m>c</m> and also check exactness at each term.
              Here we prove exactness at <m>H_n(C)</m>.
              With notation as above suppose <m>[c] = \pi_*([x])</m> then we can take <m>b = x</m>, <m>db = 0</m> implying that <m>a =0</m> and so <m>\partial [c] = [a] = 0</m>.
              We have then that <m>\im \pi_* \subset \ker \partial</m>, so it remains to show that <m>\ker \partial\subset \im \pi_*</m>.
              If <m>[a] = da',\,a'\in A_n</m> we let <m>b' = b - i(a')</m> then <m>d(b') = db - d(ia') = db - i(da') = db - db = 0</m>.
              Then <m>[b'] \in H_n(B)</m> so <m>\pi_*([b']) = [\pi(b')] = [\pi(b) - \pi(i(a'))] = [\pi(b)] = [c]</m>.
            </proof>
          </lemma>

          <exercise>
            <statement>
              Suppose we have a map of short exact sequences <todo>pic</todo> commuting, then show there is a map of long exact sequences of homology commuting <todo>pic</todo>.
            </statement>
          </exercise>

          <example>
            <title>Reduced homology</title>
            <statement>
              If <m>X</m> is a space let
              <me>
                \tilde C_*(X) = \begin{cases}C_*(X), &amp;* \ne -1,\\\ZZ, &amp; * = -1,\end{cases}
              </me>
              then define <m>d(e_p)= e</m> for <m>p \in X</m>.
              We have <m>d^2e_\gamma = d(e_{\gamma(1)} - e_{\gamma(0)}) = e - e = 0</m>.
              So <m>\tilde C_*</m> is a chain complex.
              <br />
              This construction can be motivated by thinking of <m>\Delta^{-1} = \{\}</m> and then considering <m>\Map(\{\},X) = \{e\}</m>.
              If
              <me>
                A_* = \begin{cases} \ZZ = \langle e \rangle, &amp; * = -1, \\ 0, &amp; * = -1.\end{cases}
              </me>
              Then we have a short exact sequence
              <me>
                0 \to A_* \to \tilde C_*(X) \to C_*(X) \to 0.
              </me>
              We have <m>H_*(A) = A_*</m> and so the long exact sequence of homology then says that for <m>n \ge 0</m>
              <me>
                \cdots \to H_n(A) = 0 \to H_n(\tilde C(X))\to H_n(C(X)) \to H_{n-1}(A)= 0 \to \cdots
              </me>
              giving that <m>H_n(\tilde C_*(X)) \cong H_n(C_*(X))</m> for <m>n \gt 0</m>.
              We write <m>\tilde H_*(X)</m> for <m>H_*(\tilde C_*(X))</m>. <todo>notation</todo>
              At <m>n = 0</m> we get
              <me>
                \cdots \to 0 \to \tilde H_0(X)\to H_0(X) \to \ZZ \to 0 \to\cdots
              </me>
              giving that <m>\tilde H_0(X)</m> has one fewer copies of <m>\ZZ</m> than <m>H_0(X)</m>.
            </statement>
          </example>

          <example>
            <title>Homology of a pair</title>
            <statement>
              Suppose <m>A\subset X</m>, then <m>C_*(A) \subset C_*(X)</m> is a subcomplex.
              Define <m>C_*(X,A) = C_*(X)/C_*(A)</m> and so we have a short exact sequence
              <me>
                0 \to C_*(A) \to C_*(X) \to C_*(X,A) \to 0.
              </me>
            </statement>
          </example>
        </subsection>

        <subsection>
          <title>Homology of a pair</title>

          <definition>
            <title>Homology of a pair</title>
            <statement>
              <m>H_*(X,A) = H_*(C_*(X,A))</m> is the <term>homology of the pair <m>(X,A)</m></term>.
            </statement>
          </definition>

          <p>
            From this we obtain:
          </p>

          <definition>
            <title>Long exact sequence of a pair</title>
            <statement>
              The <term>long exact sequence of the pair <m>(X,A)</m></term> is
              <me> \cdots \to H_n(A) \to H_n(X) \to H_n(X,A) \xrightarrow{\partial} H_{n-1} (A) \to \cdots.</me>
            </statement>
          </definition>

          <p>
            If <m>f\colon (X,A) \to (Y,B)</m> is a map of pairs then we get an <term>induced map</term> <m>f_\# \colon C_*(X) \to C_*(Y)</m> defined by
            <me>
              (\sigma\colon \Delta^n \to A) \mapsto f\circ \sigma.
            </me>
            Observe that <m>f_\#(C(A)) \subset C_*(B)</m> and so <m>f_\#</m> descends to a map
            <me>
              f_\#\colon C_*(X)/C_*(A) \to C_*(Y)/C_*(B)
            </me>
            or equivalently <m>f_\#\colon C_*(X,A) \to C_*(Y,B)</m> this then induces <m>f_*\colon H_*(X,A) \to H_*(Y,B)</m>.
          </p>

          <proposition>
            <title>Homotopy invariance</title>

            <statement>
              If <m>f,g \colon (X,A) \to (Y,B)</m> are homotopic as maps of pairs then <m>f_* = g_* \colon H_*(X,A) \to H_*(Y,B)</m>.
            </statement>

            <proof>
              Let <m>H\colon (X\times I, A\times I) \to (Y,B)</m> be the homotopy, <m>H</m> induces a chain homotopy <m>h \colon C_*(X) \to C_{*+1}(Y)</m> where <m>dh + hd = f_\# - g_\#</m>.
              <m>H(A\times I) \subset B</m> so <m>h(C_*(A)) \subset C_{*+1}(B)</m> this implies that <m>h</m> descends to a map
              <me>
                h \colon C_*(X)/C_*(A) \to C_{*+1}(Y)/C_{*+1}(B)
              </me>
              with <m>hd + dh = f_\# - g_\#</m> as any relation satisfied will remain satisfied in the quotient.
              So we have <m>h \colon C_*(X,A) \to C_{*+1}(Y,A)</m> and hence <m>f_\#,g_\#\colon C_*(X,A) \to C_*(Y,B)</m> are chain homotopic.
            </proof>
          </proposition>

          <subsubsection>
            <title>Visualising relative homology classes</title>

            <p>
              If <m>W^{n+1}</m> is a connected oriented compact manifold then we'll show that <m>H_{n+1}(W,\partial W) \cong \ZZ = \langle [W,\partial] \rangle</m> (where the <m>\partial</m> notation means relative to boundary)<todo></todo>.
              So given <m>f\colon (W, \partial) \to (X,A)</m> we get <m>f_*([W,\partial W]) \in H_{n+1}(X,A)</m>.
            </p>

            <example>
              <statement>
                Let <m>X = \RR^3</m> and <m>A= S^1</m> then <m>W = D^2</m> defines a class in <m>H_2(\RR^3,S^1)</m> (the boundary of <m>W</m> lies inside of <m>A</m>).
              </statement>
            </example>
          </subsubsection>
        </subsection>

        <subsection>
          <title>Good pairs</title>

          <definition>
            <title>Good pair</title>

            <statement>
              <m>(X,A)</m> is a <term>good pair</term> if
              <ol>
                <li>
                  <m>\exists U \subset X</m> open and <m>A \subset U</m>
                </li>
                <li>
                  <m>\exists \pi \colon U \to A</m> with <m>\pi|_A = \id_A</m>.
                </li>
                <li>
                  <m>\pi \sim 1_{(U,A)}</m> as maps of pairs from <m>(U,A)</m> to itself.
                </li>
              </ol>
              (i.e. <m>A</m> is a <emph>deformation retract</emph> of <m>U</m>).
            </statement>
          </definition>

          <example>
            <title>Good pairs</title>

            <statement>
              <ol>
                <li>
                  <m>(\text{smooth manifold}, \text{closed submanifold})</m>.
                </li>
                <li>
                  <m>(\text{simplicial complex}, \text{subcomplex})</m>.
                </li>
              </ol>
            </statement>
          </example>

          <example>
            <title>Not good pairs</title>
            <statement>
              <ol>
                <li>
                  <m>(\RR, \QQ)</m>.
                </li>
                <li>
                  Letting <m>H\subset \RR^2</m> be the Hawaiian earring then <m>(\RR^2, H)</m> is not a good pair.
                </li>
              </ol>
            </statement>
          </example>

          <theorem>
            <statement>
              Take <m>A\subset X</m> and let <m>\pi</m> be the natural map <m>\pi\colon(X,A)\to(X/A,A/A)\cong (X/A,*)</m>.
              Then if <m>(X,A)</m> is a good pair the induced map <m>\pi_*\colon H_*(X,A)\to H_*(X/A,*)</m> is an isomorphism.
            </statement>
            <proof>
              Postponed.
            </proof>
          </theorem>

          <exercise>
            <statement>
              The composite map <m>\phi</m> in
              <me>
                \tilde H_*(X) \to H_*(X) \to H_*(X,*)
              </me>
              is an isomorphism.
              i.e.
              <me>
                H_*(X,*) = \begin{cases} H_*(X), &amp;* \gt 0,\\H_0(X)/\ZZ, &amp;* \le 0.\end{cases}
              </me>
            </statement>
          </exercise>

          <proposition xml:id="sphere-homology">
            <statement>
              <me>
                \tilde H_*(S^n) = \begin{cases} \ZZ, &amp;* = n,\\0, &amp;* \ne n.\end{cases}
              </me>
            </statement>
            <proof>
              We proceed by induction on <m>n</m>.
              For <m>n = 0</m> we have <m>S^0 = \{p_+, p_-\}</m> implying
              <me>
                H_*(S^0) = H_*(p_+) \oplus H_*(p_-) = \begin{cases} \ZZ^2, &amp;* = 0,\\0, &amp;* \ne 0,\end{cases}
              </me>
              giving
              <me>
                \tilde H_*(S^0) = \begin{cases} \ZZ, &amp;* = 0,\\0, &amp;* \ne 0.\end{cases}
              </me>
              Now consider the long exact sequence of the pair <m>(D^n, S^{n-1})</m>
              <me>
                H_*(D^n) \to H_*(D^n, S^{n-1}) \to H_{*-1}(S^{n-1}) \xrightarrow{\phi} H_{*-1}(D^n),
              </me>
              we can break this up using the kernel and cokernel to get
              <men xml:id="sphere-homology-coker-sequence">
                0 \to \coker \phi_m \to H_m(D^n, S^{n-1}) \to \ker \phi_{m-1} \to 0.
              </men>
              <m>D^n</m> is contractible and so we get
              <me>
                H_*(D^n) = \begin{cases} \ZZ, &amp;* = 0,\\0, &amp;* \ne 0.\end{cases}
              </me>
              We have <m>\phi\colon H_0(S^{n-1}) \to H_0(D^n) \cong \ZZ = \langle e_p\rangle</m> given by <m>\phi(e_p) = e_p</m>.
              We see that <m>\ker \phi = \tilde H_*(S^{n-1})</m> and <m>\coker \phi = 0</m>.
              Now by looking at <xref ref="sphere-homology-coker-sequence"/> we see that
              <me>
                0 \to \tilde H_*(S^n) = H_*(S^n,*) = H_*(D^n, S^{n-1}) \to \ker\phi = \tilde H_*(S^{n-1}) \to 0
              </me>
              is exact giving <m>\tilde H_*(S^n) \cong \tilde H_*(S^{n-1})</m>.
              The claim then follows by induction.
            </proof>
          </proposition>

          <corollary>
            <statement>
              <me>
                S^n \sim S^m \implies m = n.
              </me>
            </statement>
          </corollary>

          <example>
            <title>Chains generating <m>H_n(S^n, *)</m></title>
            <statement>
              <ol>
                <li>
                  Choose <m>f\colon \Delta^n \to \Delta^n</m> a homeomorphism and let <m>e\colon \Delta^n \to \Delta^n</m> be the identity map.
                  Then <m>a_{n-1} = f_\#(de) \in C_*(S^{n-1})</m> and we have <m>d a_{n-1} = df_\#(de) = f_\# (d^2 e) = 0</m> and so <m>a_{n-1}</m> is closed.
                </li>
                <li>
                  Choose <m>g\colon (D^n/S^{n-1}, S^{n-1}/S^{n-1}) \to (S^n, *)</m>.
                  Now let <m>b_n = g_\#(f_\#(e)) \in C_n(S^n,*)</m>.
                  Then <m>db_n = g_\#(a_{n-1}) \in C_*(*)</m> implies <m>b_n</m> is closed in <m>C_*(S^n, *)</m>.
                  <br />
                  For example if <m>n = 2</m> we are crushing the boundary of the <m>2</m>-simplex to a point.
                </li>
              </ol>
            </statement>
          </example>

          <proposition>
            <statement>
              <m>[a_n]</m> generates <m>\tilde H_n(S^n) = \ZZ</m> (statement <m>(A_n)</m>). <br />
              <m>[b_n]</m> generates <m>\tilde H_n(S^n, *) = \ZZ</m> (statement <m>(B_n)</m>). <br />
            </statement>
            <proof>
              Statement <m>(A_0)</m>: <m>S^0 = \{p_+, p_-\}</m>, <m>de = e_{p_+} - e_{p_-}</m> so <m>de</m> generates <m>\tilde H_0(S^0)</m>.<br />
              We'll show that <m>A_{n-1}\implies B_n</m>.
              Here <m>\partial\colon H_n(S^n, *)=H_n(D^n, S^{n-1}) \xrightarrow{\sim} \tilde H_{n-1}(S^{n-1})</m> (by <xref ref="sphere-homology" /><todo>check</todo>).
              So it suffices to check that <m>\partial [b_n] = [a_{n-1}]</m>
              <todo>finish</todo>
            </proof>
          </proposition>

          <definition>
            <title>Wedge product</title>
            <statement>
              If <m>(X_i, p_i)</m> are pointed spaces the <term>wedge product</term>
              <me>
                \bigvee_{i\in I} (X_i,p_i) \text{ is } \coprod_{i\in I} X_i\Big/\{p_i : i\in I\}.
              </me>
              If <m>X_i</m> is such that for any <m>p,q\in X_i</m> there exists a homeomorphism <m>f\colon X_i \to X_i</m> with <m>f(p) = q</m> we can drop <m>p_i</m> from the notation (for example in the case of <m>X_i</m> a connected manifold we can do this).
            </statement>
          </definition>

          <example>
            <statement><todo></todo></statement>
          </example>

          <corollary>
            <statement>
              If <m>(X_i, p_i)</m> are good pairs then
              <me>
                \tilde H_*\left(\bigvee_{i}(X_i, p_i)\right) = \bigoplus_i \tilde H_*(X_i).
              </me>
            </statement>
            <proof>
              <md>
                <mrow>\tilde H_*\left(\bigvee_{i\in I} X_i\right) &amp;\cong H_*\left(\bigvee_{i\in I} X_i, p\right) \cong H_*\left(\coprod_{i\in I} X_i, \{p_i : i\in I\}\right)</mrow>
                <mrow>&amp;\cong \bigoplus_{i\in I} H_*\left(X_i, p_i\right) \cong \bigoplus_{i\in I} \tilde H_*(X_i).</mrow>
              </md>
            </proof>
          </corollary>

          <example>
            <statement>
              <me>
                H_*(S^1 \vee S^2) = \begin{cases}\ZZ, &amp;* = 0,1,2\\0, &amp;\text{otherwise}.\end{cases}
              </me>
            </statement>
          </example>
        </subsection>
      </section>

      <section>
        <title>Subdivision and Excision</title>

        <definition>
          <statement>
            If <m>\U = \{U_i\}_{i\in I}</m> is an open cover of <m>X</m>, let
            <me>
              C^{\U}_n(X) = \langle e_\sigma : \sigma\colon \Delta^n \to X,\,\im\sigma\subset U_i\text{ for some }i\rangle \subset C_n(X).
            </me><todo>notation</todo>
            Observe that <m>\im \sigma \subset U_i</m> implies <m>\im \sigma \circ F_j \subset U_i</m> and so <m>C^{\U}_*</m> is a subcomplex of <m>C_*</m>.
            Let <m>H^{\U}_*</m> be the homology of this complex, then we have a map
            <me>
              i\colon C^{\U}_*(X) \hookrightarrow C_*(X).
            </me>
          </statement>
        </definition>

        <lemma>
          <title>Subdivision</title>
          <statement>
            <m>C_*\colon H^{\U}_*(X) \to H_*(X)</m> is an isomorphism.
          </statement>
        </lemma>
        <todo>finish</todo>
      </section>

      <section>
        <title>Degree and Orientations</title>
      </section>

      <section>
        <title>Cell Complexes</title>

        <definition>
          <title>Attaching of cells</title>
          <statement>
            If <m>f\colon S^{n-1} \to X</m> then
            <me>
              X\cup_f D^n = X\amalg D^n/\sim
            </me>
            is the space obtained by <term>attaching</term> an <m>n</m>-dimensional cell to <m>X</m> via the map <m>f</m>.
          </statement>
        </definition>

        <example>
          <statement>
            If <m>X = \{p\}</m> and <m>f\colon S^{n-1} \to X</m> then
            <me>
              X\cup_f D^n\cong D^n/S^{n-1} \cong S^n.
            </me>
          </statement>
        </example>

        <definition>
          <title>Finite cell complex</title>
          <statement>
            A 0-dimensional <term>finite cell complex</term> is a finite disjoint union of points.
            <br />
            A <m>k</m>-dimensional finite cell complex is a space obtained by attaching finitely many <m>k</m>-cells to a <m>(k-1)</m>-dimensional finite cell complex.
          </statement>
        </definition>

        <example>
          <todo></todo>
        </example>

        <example>
          <statement>
            If a finite cell complex <m>X</m> has one 0-cell and one <m>n</m>-cell then <m>X \cong S^n</m>.
            Similarly if <m>X</m> has one 0-cell and <m>k</m> <m>n</m>-cells then <m>X \cong \bigvee_{i=1}^{k}S^n</m>.
          </statement>
        </example>

        <example>
          <statement>
            <m>T^2</m> is a finite cell complex with one 0-cell, two 1-cells and one 2-cell.<todo>pic</todo>
          </statement>
        </example>
 
        <example>
          <statement>
            Any simplicial complex is a finite cell complex and any closed manifold can be given the structure of a finite cell complex.
          </statement>
        </example>
      </section>
    </chapter>

    <chapter>
      <title>Cohomology and Products</title>

      <section>
        <title>Homology with Coefficients and Cohomology</title>

        <subsection>
          <title>Hom and <m>\otimes</m> for modules</title>

          <definition>
            <title>Tensor product of <m>R</m>-modules</title>
            <statement>
              Let <m>M,N</m> be <m>R</m>-modules.
              Then the <term>tensor product </term> <m>M\otimes N</m> is the <m>R</m>-modules generated by all pairs <m>m\otimes n</m> for <m>m\in M,\,n\in N</m> modulo the relations:
              <ol>
                <li>
                  <m>(m_1+m_2)\otimes (n_1 + n_2) = \sum m_i \otimes n_j.</m>
                </li>
                <li>
                  <m>r(m\otimes n) = (rm)\otimes n = m\otimes (rn).</m>
                </li>
              </ol>
            </statement>
          </definition>

          <p>
            We have the following properties of this definition:
            <ol>
              <li><m>(M_1\oplus M_2) \otimes (N_1\oplus N_2) = \bigoplus M_i\otimes N_j</m>.</li>
              <li><m>M\otimes N \cong N \otimes M</m>.</li>
              <li><m>M\otimes R = M</m></li>
            </ol>
          </p>

          <example>
            <title>Tensor products</title>
            <statement>
              <ol>
                <li><m>R^n \otimes R^m \cong R^{mn}</m>.</li>
                <li>Letting <m>R = \ZZ</m>, <m> \QQ \otimes \ZZ/a\cong 0</m>.</li>
                <li><m>\ZZ/a\otimes\ZZ/b\cong \ZZ/(a,b)</m>.</li>
              </ol>
            </statement>
          </example>

          <p>If <m>f\colon M_1 \to M_2</m> and <m>g\colon N_1 \to N_2</m> then there is a map
            <md>
              <mrow>f\otimes g \colon M_1 &amp;\otimes N_1 \to M_2 \otimes N_2,</mrow>
              <mrow>m&amp;\otimes n \mapsto f(m) \otimes g(n).</mrow>
            </md>
          </p>

          <example>
            <statement>
              If <m> f\colon R^n \to R^m</m> is given by multiplication by <m>A\in \operatorname{Mat}_{n\times m}(R)</m> then <m>f\otimes 1_M\colon M^n \to M^m</m> is given by multiplication by <m>A</m>.
            </statement>
          </example>

          <definition>
            <title>Hom</title>
            <statement>
              <me>\Hom(M, N) = \{f\colon M\to N : f \text{ is } R \text{-linear}\}</me>
              is an <m>R</m>-module, via <m>(f + rg)(m) = f(m) + rg(m)</m>.
            </statement>
          </definition>

          <p>From this definition we see that
            <ol>
              <li><m>\Hom(\bigoplus M_i, \bigoplus N_j)\cong \bigoplus_{i,j} \Hom(M_i, N_j)</m>.</li>
              <li><m>\Hom(R, M) \cong M</m>.</li>
            </ol>
          </p>

          <p>Note however that we do not have <m>\Hom(M,N) = \Hom(N,M)</m> as for example <m>\Hom(\ZZ/2,\ZZ) = 0</m> but <m>\Hom(\ZZ, \ZZ/2) = \ZZ/2</m>.</p>

          <definition>
            <title>Dual module</title>
            <statement>
              Given an <m>R</m>-module <m>M</m> the <term>dual</term> of <m>M</m> is <m>M^* = \Hom(M,R)</m>.
            </statement>
          </definition>

          <p>Now if we have <m>f\colon M \to N</m> we get a map
            <me>f^*\colon \Hom(N, O) \to \Hom(M, O)</me>
            given by <m>f^*g = g\circ f</m>.
          </p>

          <example>
            <statement>
              If <m>f\colon R^n \to R^m</m> is multiplication by <m>A</m> then
              <me>f^*\colon \Hom(R^m , O) \cong O^m \to \Hom(R^n, O) \cong O^n)</me>
              is multiplication by <m>A^\top</m>.
            </statement>
          </example>
        </subsection>

        <subsection>
          <title>Hom and <m>\otimes</m> for chain complexes</title>

          <p>If <m>(C, d)</m> is a chain complex defined over <m>R</m> then so are <m>(C_*\otimes M, d\otimes 1_M) = C_* \otimes M</m> and <m>(\Hom(C_*,M),d^*) = \Hom(C_*, M)</m>.</p>
        </subsection>
        <todo>work out whats going on here</todo>
      </section>

      <section>
        <title>Universal Coefficient Theorems</title>

      </section>

      <section>
        <title>Products</title>
        
        <definition>
          <title>Product of chain complexes</title>
          <statement>If <m>A,B</m> are chain complexes the <m>C=A \otimes B</m> is the chain complex with
            <me>C_i = \bigoplus_{j+k = i}A_j \otimes B_k</me>
            and
            <me>d(a\otimes b) = (da)\otimes b + (-1)^{|a|} a\otimes (db)</me>
            where <m>|a| = j</m> if <m>a \in A_j</m>.
          </statement>
        </definition>

        <todo>lots</todo>

        <p>The following theorem is true but we will not prove it.</p>

        <theorem>
          <title>Eilenberg-Zilber</title>
          <statement><me>C_*(X\times Y) \sim C_*(X) \otimes C_*(Y).</me></statement>
        </theorem>

        <p>Given <m>H_*(A)</m> and <m>H_*(B)</m> how can we compute <m>H_*(A\otimes B)</m>.</p>

        <lemma>
          <statement>Suppose <m>A\sim A'</m>. Then <m>A\otimes B \sim A'\otimes B</m>.</statement>
          <proof>Easy.</proof>
        </lemma>

        <corollary>
          <statement>Suppose <m>A,B</m> are finitely generated chain complexes, defined over a field. Then
            <me>H_*(A\otimes B) = \bigoplus_{j+k = i} H_j(A) \otimes H_k(B)</me>
            i.e.
            <me>H_*(A\otimes B) \cong H_*(A) \otimes H_*(B).</me>
          </statement>
          <proof><m>A</m> is a chain complex over a field, so <m>A\sim A'</m> where <m>A'_i = H_i(A)</m> and <m>d \equiv 0</m> on <m>A'</m>.
            Similarly for <m>B</m>.
            Then
            <me>H_*(A\otimes B) \cong H_*(A'\otimes B') \cong A'\otimes B' = H_*(A)\otimes H_*(B).</me>
          </proof>
        </corollary>

        <corollary xml:id="homology-product-is-tensor">
          <statement>If <m>k</m> is a field and <m>X,\,Y</m> are finite chain complexes then
            <me>H_*(X\times Y; k)\cong H_*(X; k) \otimes_k H_*(Y; k).</me>
          </statement>
        </corollary>

        <definition>
          <title>Poincare polynomial</title><todo>accent</todo>
          <statement>The <term>Poincare polynomial</term> of <m>X</m> with coefficients in <m>k</m> is
            <me>P_k(X) = \sum_{i\ge 0} \dim H_i(X; k) t^i.</me>
          </statement>
        </definition>

        <example>
          <statement>
            <me>P_{\ZZ/2} (\RP^2) = 1 + t + t^2.</me>
          </statement>
        </example>

        <p><xref ref="homology-product-is-tensor"/> tells us that <me>P_k(X\times Y) = P_k(X)P_k(Y).</me></p>

        <example>
          <statement>
            <me>P_{\ZZ/2}(\RP^3 \times \RP^2) = (1+t+t^2)(1+t+t^2+t^3).</me>
          </statement>
        </example>
        
        <p>For now we will suppose that <m>A,B</m> are finitely generated chain complexes over a PID <m>R</m>.</p>

        <lemma xml:id="free-homology-homotopic-zero-boundary">
          <statement>If <m>H_*(A)</m> is a free <m>R</m>-module then <m>A\sim A'</m> where <m>A_i' = H_i(A)</m> and <m>d\equiv 0</m> on <m>A'</m>.
          </statement>
          <proof><m>A</m> is the direct sum of short and stupid complexes. <m>H_*(A)</m> being torsion free implies that every short complex is of the form
            <me>
              \xymatrix{ C\colon R\ar@&lt;.5ex&gt;[r]^{\cdot a} &amp; R \ar@&lt;.5ex&gt;[l]^{\cdot a^{-1}}}
            </me>
            with <m>a\in R^\times</m> such a <m>C \sim 0</m> (same proof as for a field).
          </proof>
        </lemma>

        <corollary>
          <statement>If <m>H_*(A)</m> is free then <m>H_*(A\otimes B) \cong H_*(A) \otimes H_*(B)</m>.</statement>
          <proof><m>A\otimes B \cong A' \otimes B</m> where <m>A'</m> is as in <xref ref="free-homology-homotopic-zero-boundary" />.
            <m>A'\otimes B</m> is a direct sum of copies of <m>B</m>, one for each generator of <m>A'</m>.
            This gives that <m>H_*(A'\otimes B)</m> is  a direct sum of copies of <m>H_*(B)</m>, one for each generator of <m>H_*(A)</m> which implies that
            <me>H_*(A\otimes B) \cong H_*(A) \otimes H_*(B)</me>
            (since <m>H_*(A)</m> is free).
            <me>R^n \otimes H_*(B) \cong (H_*(B))^n.</me>
          </proof>
        </corollary>

        <corollary>
          <statement>If <m>X</m> and <m>Y</m> are finite cell complexes and <m>H_*(X)</m> is free over <m>\ZZ</m>, then <m>H_*(X\times Y) \cong H_*(X) \otimes H_*(Y)</m>.</statement>
        </corollary>

      </section>
    </chapter>

    <backmatter>
      <title>Reference</title>

      <section>
        <title>Notation</title>
        <notation-list />
      </section>
    </backmatter>

  </book>

</mathbook>
